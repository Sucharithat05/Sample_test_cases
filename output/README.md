# UI Explorer locator outputs

This folder is the **only** place discovered locators are stored.

| File | Contents |
|------|----------|
| `ui_explorer_<TEST_CASE_ID>.json` | Per-step locators found while running that Markdown test case |

Generated by the UI Explorer Agent. Do not copy these locators back into `test-cases/*.md` — those files describe intent only.
